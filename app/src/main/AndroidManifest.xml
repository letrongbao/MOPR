<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".core.MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">

        <!-- Auth -->
        <activity
            android:name=".features.auth.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".features.auth.SignUpActivity" />

        <!-- Home -->
        <activity android:name=".features.home.HomeMenuActivity" />
        <activity android:name=".features.home.HomeActivity" />

        <!-- Property -->
        <activity android:name=".features.property.house.CanNhaActivity" />
        <activity android:name=".features.property.room.PhongTroActivity" />
        <activity android:name=".features.property.room.ChiTietPhongTroActivity" />
        <activity android:name=".features.property.room.LichSuCongToActivity" />

        <!-- Invoice -->
        <activity android:name=".features.invoice.HoaDonActivity" />
        <activity android:name=".features.invoice.LichSuThanhToanActivity" />

        <!-- Tenant / Contract -->
        <activity android:name=".features.tenant.NguoiThueActivity" />
        <activity android:name=".features.contract.HopDongActivity" />
        <activity android:name=".features.contract.ContractPdfPreviewActivity" />
        
        <!-- History -->
        <activity android:name=".features.history.RentalHistoryActivity" />

        <!-- Finance -->
        <activity android:name=".features.finance.ChiPhiActivity" />
        <activity android:name=".features.finance.DoanhThuActivity" />

        <!-- Settings -->
        <activity android:name=".features.settings.ProfileActivity" />
        <activity android:name=".features.settings.EditProfileActivity" />
        <activity android:name=".features.settings.ChangePasswordActivity" />
        <activity android:name=".features.settings.BackupRestoreActivity" />

        <!-- Org -->
        <activity android:name=".features.org.OrgAdminActivity" />

        <!-- Tickets -->
        <activity android:name=".features.ticket.TicketActivity" />

        <service
            android:name=".core.service.ImageUploadService"
            android:foregroundServiceType="dataSync" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
